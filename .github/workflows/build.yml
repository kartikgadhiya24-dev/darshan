name: Build Native Apps

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-shared-rust:
    name: Build Shared Rust Core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Build Shared Core
        run: |
          echo "Placeholder for Cargo Build"
          # cd shared && cargo build --release

  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    needs: build-shared-rust
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Build Android
        run: |
          echo "Placeholder for Android Gradle Build"
          # cd android && ./gradlew assembleDebug

  build-windows:
    name: Build Windows Host
    runs-on: windows-latest
    needs: build-shared-rust
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'
      - name: Build Windows App
        run: |
          echo "Placeholder for Windows Build"
          # cd host/windows && dotnet build -c Release
